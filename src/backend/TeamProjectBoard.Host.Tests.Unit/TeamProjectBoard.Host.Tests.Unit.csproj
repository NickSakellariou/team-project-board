<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <IsPackable>false</IsPackable>
        <GenerateDocumentationFile>false</GenerateDocumentationFile>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Microsoft.NET.Test.Sdk" />
        <PackageReference Include="Microsoft.Extensions.Diagnostics.Testing" />
        <PackageReference Include="xunit" />
        <PackageReference Include="xunit.analyzers" />
        <PackageReference Include="xunit.runner.visualstudio" />
        <PackageReference Include="coverlet.collector" />
    </ItemGroup>

    <!--
      The Host's own unit tests, in the sense of "no database and no HTTP": the suite that
      drives the running application is Modules.Users.Tests.Integration. Right now that
      means HostLogs, which needs a test file like every other log catalogue (see
      docs/log-event-ids.md) and has no business waiting on a Postgres container to run.
    -->
    <ItemGroup>
        <ProjectReference Include="..\TeamProjectBoard.Host\TeamProjectBoard.Host.csproj" />
    </ItemGroup>

</Project>
