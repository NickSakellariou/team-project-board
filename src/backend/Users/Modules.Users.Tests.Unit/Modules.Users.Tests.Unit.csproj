<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <IsPackable>false</IsPackable>
        <GenerateDocumentationFile>false</GenerateDocumentationFile>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="Microsoft.NET.Test.Sdk" />
        <PackageReference Include="NSubstitute" />
        <PackageReference Include="xunit" />
        <PackageReference Include="xunit.analyzers" />
        <PackageReference Include="xunit.runner.visualstudio" />
        <PackageReference Include="coverlet.collector" />
    </ItemGroup>

    <ItemGroup>
        <ProjectReference Include="..\Modules.Users.Features\Modules.Users.Features.csproj" />
    </ItemGroup>

    <!-- Handlers are internal by design. The InternalsVisibleTo that lets this project
         see them is declared in Modules.Users.Features.csproj, since the grant has to come
         from the assembly that owns the types. -->

</Project>
